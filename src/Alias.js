import React, { Component } from 'react';
import './Alias.css';

class Alias extends Component {
    render() {
        return (
            <div className="Alias">
            Enter Alias
            </div>
        );
    }
}

export default Alias;
